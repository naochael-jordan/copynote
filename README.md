# copynote
clipboard manager(Electron) for Mac

## SETUP

``` bash
$ npm install(or yarn)

# start electron
$ npm run start

# watch the src directory and output it to the dist directory
$ npm run watch
```
